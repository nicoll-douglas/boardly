const MAX_FILE_SIZE = 2 * 1024 * 1024; // 2MB
const ALLOWED_TYPES = ["image/jpeg", "image/png"];
const MAX_DIMENSIONS = { width: 500, height: 500 };

export { MAX_DIMENSIONS, MAX_FILE_SIZE, ALLOWED_TYPES };
